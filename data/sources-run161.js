(() => {
  // Canonical Toridoll recommendation source is supplied by the currently loaded
  // legacy article shard. Keep this source shard inert so the same source ID and
  // canonical URL are not registered twice before articles-run161.js applies its
  // topic/article updates.
})();
